apiVersion: v1
kind: Service
metadata:
  name: clembot-site
  namespace: clembot
  labels:
    app: clembot-site
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: clembot-site
