apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clembot-api
  namespace: clembot
  annotations:
    external-dns.alpha.kubernetes.io/ingress-hostname-source: annotation-only
    external-dns.alpha.kubernetes.io/hostname: api.clembot.io
spec:
  rules:
  - host: api.clembot.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: clembot-api
            port:
              number: 5000
