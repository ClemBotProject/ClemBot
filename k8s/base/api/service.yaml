apiVersion: v1
kind: Service
metadata:
  name: clembot-api
  labels:
    app: clembot-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: clembot-api
